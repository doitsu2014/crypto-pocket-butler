/// Application DTOs (Data Transfer Objects)
///
/// Request and response types used by the API handlers.
/// These are kept separate from domain types to preserve the domain's
/// independence from HTTP concerns.

pub mod account;
pub mod portfolio;
